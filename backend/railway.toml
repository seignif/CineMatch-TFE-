[build]
builder = "nixpacks"

[deploy]
startCommand = "/app/venv/bin/python manage.py migrate --noinput && /app/venv/bin/daphne -b 0.0.0.0 -p $PORT config.asgi:application"
healthcheckPath = "/api/health/"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
